import { motion } from "framer-motion";


interface props {
  children?: string;
}

export default function LetterAnimation({children = "placeholder"} : props) {
  const split = children.split("");
  
  return (
    <>
      
    </>
  );
}